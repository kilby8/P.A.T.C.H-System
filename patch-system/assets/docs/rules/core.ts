import { RulesSection } from './types';

export const CORE_RULES_SECTION: RulesSection = {
  title: 'Core Game Rules',
  rules: [
    'Define the Big Three first: genre and tone, core gameplay loop, and unique x-factor.',
    'Choose one resolution engine that supports your tone before adding content.',
    'Build a character skeleton first: attributes, archetypes, and progression hooks.',
    'Constrain worldbuilding prompts so lore stays specific instead of generic.',
    'Start with a quickstart scope that can run one focused scene end-to-end.',
  ],
};
